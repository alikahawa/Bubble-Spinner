package game;

/**
 *  Enum with the different types of events the game can trigger.
 *  Can be expanded on later.
 *
 * @author Group-13
 */
public enum UpdateType {
    GRID,SHOT,MOVE,DESTROY, WIN, BOUNCE, LOSE
}
